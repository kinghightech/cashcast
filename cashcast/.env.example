# Optional: enables richer holiday + food/cultural observance data via Calendarific.
# Without this key, Nager.Date is used as a fallback for public holidays.
CALENDARIFIC_API_KEY=

# Optional: enables nearby event signal via Ticketmaster Discovery API.
TICKETMASTER_API_KEY=

# Optional: backend port override.
PORT=8787
